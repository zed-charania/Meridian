# Render deployment configuration
services:
  - type: web
    name: meridian-pdf-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    plan: free
